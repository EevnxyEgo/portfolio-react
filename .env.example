# Web3Forms access key for the contact form — free at https://web3forms.com (just verifies your email).
# Copy this file to `.env`, paste your key, and the contact form sends real email.
# Without a key, the form validates input and falls back to a mailto: link.
VITE_WEB3FORMS_KEY=
